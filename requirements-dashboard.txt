# portfolio-dashboard.service (dashboard.py + onboarding.py, Streamlit, port 8502) —
# own venv (venv_dashboard), split off 2026-08-05 from the previously shared venv/.
# No auth/alpaca-trade-api deps here (dashboard.py has no login of its own, no
# Alpaca connect UI) — this is exactly the isolation that prevents a future
# api.py-only dependency change from downgrading streamlit/yfinance here again.
streamlit>=1.35.0
plotly>=5.20.0
anthropic>=0.25.0
yfinance>=0.2.40
pandas>=2.0.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
python-dotenv>=1.0.0
pytz>=2024.1
fpdf2>=2.7.9
pypdf>=4.2.0
pdf2image>=1.17.0
cryptography>=42.0.0
